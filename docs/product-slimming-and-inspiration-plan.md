# Syno 产品精简与今日灵感执行计划

状态：IN_PROGRESS（Owner 2026-09-01 经 /goal 批准并指示一次性执行完全部任务）
创建：2026-09-01（Asia/Shanghai）

## 1. 方向与范围

Owner 2026-09-01 决策：产品范围收敛为**搜索、收录、深度探索分析、知识库查找、今日灵感**（整合知识库文章产出串联/新观点的"做梦"功能）。学习系统与项目管理不在范围内；`/project` 显式指令被明确认为对主人不好理解，目标是单一自然语言入口。

**保留（产品面）**：搜索/知识库查找（`knowledge.search`/`read_snippet`/`fetch_url`）、完整收录管线、深度探索分析（chat 多轮工具循环 + `web_search`，本期不扩建）、今日工作台 `today.read`（暂留，Phase D 评估是否与灵感合并）、`image.read`、主动推送设施。

**保留（承重结构，不因精简删除）**：Job/PendingDecision 治理链（`jobs.submit` 是唯一受控写入口，`jobs.list` 保留为唯一通用只读运行视图）、ToolRegistry/Policy/GitGuard、Outbox、AcceptedRequest、渠道层与 ChannelConversationHandler 其余职能、DSH 运行时、Settings、claims/evidence 隐藏工具、goals（内部使用）。

**移除（先摘牌、后物理删）**：学习系统（`learning.*`、复习提醒、teach-back）、项目管理（`projects.*`、`/project` 指令、检索 `PROJECT_BOOST`、projectRef 传播）。

## 2. 冻结决策

- **D1 摘牌方式**：只改 `config/deepseek-harness/syno-tool-sets.mjs` 的 `CORE_CHAT_TOOL_NAMES`，移除 `learning.due`、`learning.teach_back`、`learning.submit`、`projects.list`、`projects.create`、`projects.update_status`；源码、schema、vault/ops 历史数据全部保留，完全可逆。
- **D2 学习休眠**：Phase A 同时停止装配 `ReviewReminderSource`（runtime 传入 null；`channel-conversation-handler.mjs` 已验证 null 时回落普通对话），复习提醒与 teach-back 软门随之休眠。不删文件。
- **D3 分支处置**：`feat/project-aware-dsh-phase5` 领先 `main` 34 个提交且 `main` 是其祖先（可 ff，2026-09-01 实测）。Owner 批准后**本地 ff-merge 到 main**（携带 host 自动提交的 ops 运行记录，不丢 Job 事实源），随后各阶段在 `main` 上进行。沿用现规：不 Push。
- **D4 Phase 5 处置**：project-aware 计划的 Phase 5 标记 `SUPERSEDED`（产品方向变更，Owner 主观召回观察不再回填）；`ops/acceptance/project-aware-knowledge-mvp/` 证据原样保留为历史记录。
- **D5 观察期**：摘牌合并后起算 2~4 周（最迟 2026-09-29 复盘）。复盘三选一：物理删除（进 Phase C）/ 恢复暴露 / 部分保留。**只有 Owner 明确复盘结论才允许进 Phase C。**
- **D6 物理移除范围（Phase C，观察期通过后）**：`learning-service.mjs`、`review-reminder-source.mjs`、teach-back 软门与「跳过复习」出口、R4.7 的 LearningCandidate/ReviewOpportunity 生成、learning 相关 Skill 章节；`projects.*` 工具、`project-directive.mjs` 与 handler 内 `/project` 链路、`project-service.mjs` 与 schema、KnowledgeStore `PROJECT_BOOST`、Job/Workflow/Proposal 的 projectRef 传播。vault/ops 历史记录（含 `ops/projects/*.md`）保留只读不删。**同步更新 `scripts/check-active-docs.mjs` 的 required 锚点**（当前强制 `docs/ARCHITECTURE.md`、`docs/POLICY.md` 含 `projects.*`）及这两份文档本身。
- **D7 今日灵感边界**：新 signal source 接入既有 ProactiveOrchestrator（安静时间 22:30–07:30、每日预算 minimal/balanced/active=1/2/3 条不变）；采样=近期收录+久未回访笔记混合；生成走 DSH proactive Session（不占 main Session）；灵感卡片持久化优先复用 OutputOpportunity store 加 `kind` 区分，schema 不兼容则独立 `ops/content/inspirations/` 并在任务 #9 记录决策；Owner 反馈入口为「有用/没用」确定性路由（不过模型）；生成失败遵守终态三件套（终态 + maxAttempts + recordEvent），禁止静默无限重试；不新造调度轮子。
- **D8 停止条件**：任一阶段若需要删除 Job 治理链、改动渠道去重/Outbox 语义、批量改写 vault 历史笔记、或未复盘就物理删除，立即记录 `BLOCKED_SCOPE_DEVIATION` 并暂停回 Owner。
- **D9 用户入口边界加固（Owner 2026-09-01 追加，无需观察期直接执行）**：用户入口只允许更改知识库。物理删除 `code_change` 意图、`policy.allowSelfModify`/`policy.allowSystemControl` 开关与 `windows.service.install/uninstall` 操作注册；Policy 无条件拒绝触及源码根（apps/contracts/config/scripts/tests）的 diff；SettingsRegistry 相关条目同步清除。删除后不存在任何配置能让聊天入口改代码或控制系统。本约束只管产品用户入口，不影响开发 Agent 的人工开发流程。
- **D10 写入根与提交闸门**：Job 中模型可声明的写入路径只允许 `vault/**`；`ops/` 收窄为系统内部固定记录器写入（Job/Workflow/事件记录），不接受模型声明路径。自动提交路径增加结构性硬闸门：只准暂存 `vault/**`、`ops/**`，源码根永不可被产品路径暂存；维持永不自动 Push。
- **D11 仓库拆分（方向已定：拆，Phase E 独立执行）**：代码、知识库、运行记录共用仓库的模糊已实际发作（host 把 ops 记录自动提交到功能分支）。长期主义决策：`vault/` + `ops/` 整体迁出为独立知识仓库，管家只碰知识仓库；代码仓库只留 apps/contracts/config/scripts/tests/docs。Phase E 设计点：vault+ops 合一仓库（最简迁移）；git 历史倾向新起点+旧历史归档 bundle（filter-repo 为备选）；Windows 下 junction 保持 vault 原位透明；cutover 需短暂停 host 冻结写入。因牵动 PATHS/备份/verify/fresh clone/文档，排在摘牌与拆墙完成之后；D10 的提交闸门保留为永久双保险。
- **D12 今日灵感设计冻结（#9，2026-09-01，实证后冻结）**：做梦机制接入既有 ProactiveOrchestrator，不新造调度轮子。实证依据：SignalEngine 的 morning/evening/weekly 定时模式、`#runAgent` 已走 proactive Session、`#deliverBundle` 已含凭据脱敏门与 outbox 状态机、`output-opportunity.schema.json` 为 `additionalProperties: false` 且 status/format 枚举是创作生命周期、`isActionableOutput` 会把 suggested 记录错报为「推进创作」信号。
  - **信号与调度**：SignalEngine 新增每日定时种类 `inspiration`（与 morning/evening 同模式）：本地时间过 12:30（2026-09-02 Owner 自 16:00 调整——午时更符合阅读作息；分钟精度，tick 60s 即首拍出卡）且当日未投递（`lastRuns[inspiration:<date>]`）时发出；共享每日预算（1/2/3 条）与安静时间（22:30–07:30），均不变。orchestrator slot 列表加 `inspiration`；slot 为 inspiration 的 bundle 走灵感卡片分支，不进 `bundlePrompt` 行动摘要。已知降级：若宿主跨 slot 停机导致灵感与其它信号混bundle，slot 优先级 morning/evening/weekly 在前，当日灵感降级为普通行动行、不产卡——宁可少一张卡，不混文案。
  - **采样（素材）**：新 `InspirationSampler` 从 KnowledgeStore 采 4 篇：2 篇近期收录（`date` 最新，7 天内优先）+ 2 篇久未回访。KnowledgeStore 无回访元数据，「久未回访」以灵感仓自身历史为准——近 30 天未被采过的笔记中 `date` 最旧优先。只采 `searchable` 内容笔记并排除 `sensitive`；不足 2 篇则当日安静跳过（recordEvent `inspiration.sample.insufficient`，不投递）。
  - **生成**：slot=inspiration 时以采样笔记的标题+excerpt+tags 组提示词，要求模型输出 ≤240 字卡片（一个串联或新观点 + 涉及笔记）；走既有 proactive Session（threadKey `proactive`），不占 main Session。模型失败**不投递空卡、不做无模型兜底文案**（标题罗列不是做梦）：attempts+1、recordEvent `inspiration.generate.failed`，后续 tick 重试，当日 maxAttempts=8 后终态 `failed_terminal`（当日不再试，次日重新 eligible）——终态+maxAttempts+recordEvent 三件套，cap 在 send 之前。
  - **持久化（D7 决策点落槌）**：output-opportunity schema 实证不兼容 → 独立仓 `ops/content/inspirations/<id>.md` + 新契约 `contracts/inspiration.schema.json`。字段：`id`（`inspiration-<yyyymmdd>-<8hex>`）、`date`、`sampledRefs`、`text`、`status`（`generated/delivered/feedback`）、`feedback`（`useful/not_useful`，缺省无）、`feedbackAt`、`deliveryEventId`、`attempts`、`created/updated`。灵感仓同时充当采样记忆（采样历史即各记录 sampledRefs∪created），不另建索引。validator 路由 `ops/content/inspirations/*.md → inspiration`。
  - **反馈路由（不过模型）**：ChannelConversationHandler 新增确定性出口——仅当存在「已投递、feedback 未填、投递 24h 内」的灵感卡，且消息**全文**匹配 `/^(有用|有启发|没用|没启发|一般)[!！。~]*$/` 时拦截：写入 feedback + 固定回执。否则完全不拦截，正常对话零影响。v1 只记录反馈，不回调采样权重（调权重待 #11 验收数据）。
  - **投递**：复用 `#deliverBundle` → ChannelDeliveryOutbox（sourceType `proactive_bundle`），继承脱敏门、web 审计、wakeDelivery 与投递状态机；微信换行走既有 formatWx/send egress 双层 `\n\n` 覆盖。卡片固定格式：标题 `Syno · 今日灵感`、正文（模型文本）、「涉及笔记」标题列表、结尾「回复『有用』或『没用』帮我调准」。
  - **today.read 评估结论（§1 暂留项）**：v1 不合并——推送已覆盖触达，灵感仓是单一事实源；TODAY 视图双写的一致性成本高于收益。#11 验收后再议。
  - **验收（#11）**：Owner 连续数日真实收到卡片并回填「有用/没用」；脱敏证据存 `ops/acceptance/inspiration/`。
- **D13 拆库设计冻结（#14，2026-09-01，全量探针+junction 实证后冻结，细化并部分修正 D11 设计点）**：
  - **D13.1 物理迁出，弃用 junction（对 D11 的实证修正）**：`D:\tmp\junction-probe2` 实测——checkout 含 `vault/` 的历史提交会穿透 junction 把历史快照写进知识库目录（离枝时又按索引删除，同名真实知识文件即被覆盖后删除=数据丢失通道）；junction 本身 `git status` 干净、`git clean -fdx` 与 `fs.rm recursive` 均只动链接不伤目标（已实测），但历史分支切换不可控。且 validator `readHeadText`（`git show HEAD:vault/...`）与 GitGuard 提交必须作用于知识仓自身的 git，junction 无法提供——代码无论如何都要显式知道知识根，junction 只剩人类便利性却引入 verify 遍历特例与分支切换地雷，奥卡姆否决。Obsidian 改开知识仓路径（cutover runbook 一步）。
  - **D13.2 单一解析入口**：`paths.mjs` 新增 `knowledgeRoot = path.resolve(process.env.SYNO_KNOWLEDGE_ROOT || REPO_ROOT)`，`vaultRoot`/`opsRoot` 改由 knowledgeRoot 派生；新增 `relativeToKnowledge`（镜像 `relativeToRepo`，对 knowledgeRoot 求相对）。env 缺省=单库模式：未设置时行为与今日逐字节一致，测试与 fresh clone 零变化。
  - **D13.3 知识仓布局原样**：`<知识仓>/vault/**` + `<知识仓>/ops/**` 平移，全部逻辑路径（recordPath、changedPaths、D10 闸门 `vault/**`/`ops/**`、validator `vault/` 过滤、知识仓内 `config/` 不建）逐字节不变；`git init -b main`，初始提交即新起点；`.gitignore` 携带 quarantine 例外与 `.obsidian/` 等规则。
  - **D13.4 GitGuard 指向知识仓**：构造默认 `repoRoot` 改为 `PATHS.knowledgeRoot`（server.mjs/runtime.mjs 两处装配不传参即得）；锁文件恒落代码仓 `.runtime/locks/`（repoRoot 为 PATHS.repoRoot 或 PATHS.knowledgeRoot 时；测试显式 temp 根仍用仓内锁保持隔离）；worktree 仍落代码仓 `.worktrees/`（git worktree 可在仓外，知识仓零杂物）；新增可选 `productBranch` 断言——两处产品装配传 `main`，当 `cwd===repoRoot`（主检出）且当前分支非 main 时拒绝产品提交，结构性堵死「host 把 ops 记录提交到功能分支」的实证误伤类；worktree 内 `syno/job/*` 分支提交不受影响（断言仅当 cwd 为主检出）。
  - **D13.5 validator 双根**：`validateRepositoryChange({ repoRoot = PATHS.knowledgeRoot, contractRoot = PATHS.repoRoot, ... })`——vault 扫描与 `readHeadText`（git HEAD 比对）作用于知识仓；`config/vault-contract.json` 留代码仓（与 `contracts/*.schema.json` 同例：由代码执行的知识治理策略，策略即代码）。测试调用点显式传 `contractRoot`。
  - **D13.6 workspace 默认值翻转**（探针确认的全部消费点，只换根不换逻辑路径）：agent-host 执行工作区与 git 快照/提交 cwd（162/166/167/305/366/368）、ingest apply（491）、runtime.mjs（693 workspace、358 migration repoRoot、1850 改 `PATHS.opsRoot`）、server.mjs（315/340/421 工作区默认、487/501 legacyWorkspace run、589 vaultRoot 归属断言、772–778 isPathInside 与 remap 基准、407 展示相对路径）、knowledge-profile-service（46 repoRoot 读 note markdown）、workflow-context-compiler（103；BASE_SOURCES 实证全为 `vault/99-System/**` 知识文件）、job-store/knowledge-store/reports 的相对路径解析改 `relativeToKnowledge`。
  - **D13.7 代码根保持不变项**：repo 指纹（server.mjs:46、runtime.mjs:88）、show-toast.ps1、DSH config/launcher/supervisor（`SYNO_REPO_ROOT` 继续传代码仓——实证 skills 无绝对 vault 路径、Tool Bridge 经 host 解析，DSH 侧零改动）、`check-active-docs.mjs`（无 vault/ops 引用）。
  - **D13.8 verify 双模式**：`verify-repository.mjs` required 清单永久移除 `vault/AGENTS.md`、`ops/README.md`（fresh clone 不再含知识内容）；`SYNO_KNOWLEDGE_ROOT` 设置时追加知识仓形态检查（根存在、含 `vault/`+`ops/`、是 git 仓、当前分支 main）。
  - **D13.9 scripts 归置**：`vault-migrate.mjs` 默认根改 `PATHS.knowledgeRoot`、runtimeRoot 改 `PATHS.runtimeRoot/migrations`（manifest 不落知识仓）；state-archive/audit-ingest 走 stateRoot 不受影响；r6-readiness/cutover-mobile-delivery 为历史验收工具保持原样。
  - **D13.10 planner 设置迁移**：cutover 时把持久化 `topic-planner.config.json`（gitignored）的 vaultRoot 改为知识仓路径（server.mjs:589 断言基准翻转后旧值会被拒绝）。
  - **D13.11 历史与备份**：知识仓新起点；代码仓历史即旧知识内容档案（runbook 注明「不要从拆库前分支启动 host」）；cutover 前 `git bundle create --all` 全量备份存仓外；filter-repo 提取知识历史为备选，本期不执行。知识仓默认无远端（是否建远端由 Owner 另行决定）。
  - **D13.12 cutover 窗口（#15）**：停 host 冻结写入 → 复制 vault/ops 至知识仓并校验文件数 → 知识仓初始提交 → 代码改动（D13.2~D13.9）先行回归（单库模式全绿）→ 代码仓 `git rm -r vault ops` + `.gitignore` 防御性 `vault/`/`ops/` 条目 → 设置用户级 `SYNO_KNOWLEDGE_ROOT` + planner vaultRoot → 重拉 host → 冒烟（health、知识搜索、一次收录提交落知识仓 main、host 活动后代码仓 `git status` 为空=拆库目标的结构性证明）。回滚预案：unset env + revert 删除提交。
  - **D13.13 验收（#16）**：全量回归前后两轮、fresh clone、verify 双模式、live 冒烟证据落 `ops/acceptance/repo-split/`、文档封板（AGENTS/INDEX/OPERATIONS/KNOWN-LIMITATIONS/NEXT_SESSION/README + 知识仓 README）、三轴复审。

## 3. 阶段与退出条件

| Phase | 内容 | 会话任务 | 退出条件 |
|---|---|---|---|
| A 摘牌 | 工具集摘牌 + 学习提醒休眠 + 边界加固（D9/D10）+ 分支 ff-merge + 回归 + 文档同步 | #2~#4、#12、#13 | `pnpm test` 全绿、`pnpm run verify` 通过、NEXT_SESSION/KNOWN-LIMITATIONS/INDEX 同步、Phase 5 关闭记录完成 |
| B 观察 | 2~4 周无学习/项目入口的真实使用，Owner 复盘 | #5 | Owner 明确复盘结论 |
| C 拆墙 | 物理移除学习+项目、`check-active-docs` 锚点更新、三轴复审、封板 | #6~#8 | 全量回归 + fresh clone 通过，Standards/Spec/Security 未解决 P0/P1 为 0，文档封板 |
| D 灵感 | 设计冻结 → 实现 → 真实验收 | #9~#11 | Owner 连续数日收到灵感推送并评价质量后封板 |
| E 拆库 | vault+ops 迁出为独立知识仓库：设计冻结 → 迁移与 cutover → 验证封板 | #14~#16 | 管家全部读写落在知识仓库，代码仓库零产品写入；回归与文档封板 |

`DONE` 定义沿用：代码完成、契约测试通过、全量测试通过、verify 通过、文档同步、阶段验收完成。自动测试、探针与 Owner 真实验收不互相替代。

## 4. 证据与文档规则

- 本计划批准后成为唯一执行事实源；阶段状态、实际修改文件、测试结果、commit hash、Owner 结论与 deferred 只追加到本文件「执行记录」区。
- 每阶段交付必须附：精确暂存路径、`pnpm test` 当轮数字、`pnpm run verify` 当轮输出、`git diff --check` 结果；不沿用历史数字。
- `scripts/check-active-docs.mjs` 的 required 锚点在 Phase C 移除项目时同步更新，不得让 verify 红着进下一阶段。
- 提交按 Job 声明精确路径暂存；禁止 `git add -A`；不自动 Push。

## 5. 执行记录（追加区）

- 2026-09-01：计划起草（DRAFT）。背景：Owner 明确产品方向收敛，学习/项目摘牌待删，Phase 5 Owner 观察 superseded。实证依据：`main` 为 HEAD 祖先（34 commits ahead，可 ff）；`channel-conversation-handler` 的 `reviewReminders=null` 回落路径存在；`check-active-docs.mjs` required 锚点强制 ARCHITECTURE/POLICY 含 `projects.*`；ProactiveOrchestrator 安静时间 22:30–07:30、cadence 预算 1/2/3 条/日。待 Owner 批准。
- 2026-09-01：Owner 追加边界约束（用户入口只允许改知识库；删除 code_change 与自修改开关；写入根收缩；提交硬闸门；知识仓库拆分方向已定），形成 D9~D11 与 Phase E（任务 #12~#16）。
- 2026-09-01：Owner 经 /goal 批准计划并指示**一次性执行完全部任务**。据此：①D5 观察期的复盘结论视为已给出（删除），Phase A 摘牌与 Phase C 拆墙合并为一次物理移除，不再经过"仅摘牌"中间态（奥卡姆：同一子系统不拆两次）；②Phase 5 直接关闭为 SUPERSEDED；③任务 #5 标记为已被 Owner 决策取代。执行顺序调整为：边界加固（#12/#13）→ 移除学习（#6）→ 移除项目（#7，含工具集）→ 封板（#3/#4/#8）→ 灵感（#9~#11）→ 拆库（#14~#16）。
- 2026-09-01：边界加固①完成（#12，`e49a66c`，15 files +56/−240）：物理删除 `code_change` 意图、`policy.allowSelfModify`/`policy.allowSystemControl` 开关与 `windows.service.install/uninstall` 操作注册；Policy 无条件拒绝触及源码根（apps/contracts/config/scripts/tests）的 diff；SettingsRegistry 相关条目同步清除。此后不存在任何配置能让聊天入口改代码或控制系统。
- 2026-09-01：边界加固②完成（#13，`b634efd`，6 files +53/−19）：Job 中模型可声明的写入路径只允许 `vault/**`；`ops/` 收窄为系统内部固定记录器写入，不接受模型声明路径；自动提交路径增加结构性硬闸门——只准暂存 `vault/**`、`ops/**`，源码根永不可被产品路径暂存；维持永不自动 Push。
- 2026-09-01：拆墙①完成（#6，`f23954d`，47 files +151/−1439）：学习子系统整体物理移除——LearningService、ReviewReminderSource、`learning.due/teach_back/submit` 三工具、`learning.evidence.record` 操作、teach-back 门与「跳过复习」出口、learning 契约与 syno-learn/syno-review Skill、Web Learn 区与每日复习设置。消化动作改为重读串联（planner digest → knowledge 区）；历史 ops 记录原样保留可读。当轮全量回归与 verify 通过后提交。
- 2026-09-01：拆墙②完成（#7，`ec068b9`，44 files +145/−2560）：项目子系统与 `/project` 链路整体物理移除——project-service/project-directive、project.schema.json、`projects.*` 三工具、Job/Workflow/Proposal/Note 的 projectRef 传播、KnowledgeStore `PROJECT_BOOST`。持久键兼容：效应幂等键保留 `<none>` 段字面量，跨重启去重连续；历史 Job/Workflow/Note 的 projectRef/project_refs 字段保留只读（契约留 optional 属性供旧记录 round-trip，读取方忽略，不再写入）。错误码更名为 `INGEST_WORKFLOW_OWNER_MISMATCH`/`WORKFLOW_JOB_OWNER_MISMATCH`（均终态）。当轮回归 699/699 通过，`pnpm run verify` 通过（1651 files、9 docs），`git diff --check` 通过。
- 2026-09-01：封板（#8，本提交）：①`docs/project-aware-knowledge-execution-plan.md` 整体关闭为 SUPERSEDED（D4），Phase 5 行同步关闭，证据保留只读；②INDEX 重分类——该计划移入 Historical，本计划成为唯一执行事实源（IN_PROGRESS）；③NEXT_SESSION 重写 2026-09-01 交接（旧 Phase 5 交接降格历史）；④KNOWN-LIMITATIONS 失效断言修正（Project 边界、BugReport/ImprovementProposal、学习证据等）；⑤`check-active-docs.mjs` activeFiles 8 files，project-aware 计划移出并仅保留 INDEX 追溯锚点。三轴复审：Standards——apps/config 无 projects.*/learning.* 悬挂引用（仅 syno-tool-bridge 兼容注释）；Spec——历史记录 round-trip 兼容经 #7 测试锁定；Security——移除净减暴露面，Owner 隔离改为 assertWebJobOwner 属主校验，无新增权限。封板回归当轮实测：`pnpm test` 699/699 pass、0 fail；`pnpm run verify` 通过（1651 files、8 docs）；`git diff --check` 通过。运行态备注：8888 Host 仍运行拆墙前代码，重启与 #9~#11 部署合并为一次。至此 Phase A/C（#2~#8、#12、#13）全部关闭，进入 Phase D（#9~#11 今日灵感）。
- 2026-09-01：封板补充证据——fresh clone（`.runtime/fresh-clone-1b66dec`，HEAD `1b66dec`）：`pnpm install --frozen-lockfile` 成功（4.1s），`pnpm test` 699/699 pass、0 fail。Phase C 退出条件（全量回归 + fresh clone + 三轴 P0/P1=0 + 文档封板）全部满足。
- 2026-09-01：今日灵感①设计冻结完成（#9，`c5fa1bf`）：实证 ProactiveOrchestrator/SignalEngine/OutputService/KnowledgeStore 现状后冻结 D12——独立灵感仓 `ops/content/inspirations/`（D7 决策点：output-opportunity schema `additionalProperties:false` + 创作生命周期枚举 + `isActionableOutput` 误报风险，实证不兼容）；SignalEngine 新增每日 `inspiration` 定时种类（16:00，Owner 2026-09-02 调整为 12:30 出卡；共享预算与安静时间不变）；InspirationSampler 采 4 篇（2 近期 + 2 久未回访，回访记忆即灵感仓历史）；生成失败不投递空卡、当日 maxAttempts=8 后终态；「有用/没用」24h 全文精确匹配的确定性反馈路由（v1 不回调权重）；today.read v1 不合并（暂留项评估落槌）。
- 2026-09-01：今日灵感②实现完成（#10，`67cb311`，10 files +657/−32）：契约+仓+采样器+编排分支+反馈路由全链路落地，测试 `tests/inspiration.test.mjs` 11 例。实现期两处实证修正：①D12 中「validator 路由 ops/content/inspirations」未实施——灵感仓是系统内部固定记录器（D10），writeRecord 落盘时已按 schema 校验，与 OutputService opportunities 无 contractForPath 路由同例；②反馈枚举扩展为 useful/neutral/not_useful（「一般」→neutral，避免二值化丢失信号）；③采样两池均排除近 30 天已采样笔记（做梦永远用新鲜素材，宁缺毋重复）；④混 bundle 降级行的行动文案为「并入本次摘要，明日单独出卡」。当轮回归 710/710 通过，`pnpm run verify` 通过（1655 files、8 docs），`git diff --check` 通过。
- 2026-09-01：今日灵感③进行中（#11）：自动部分完成——回归 710/710、verify 1655/8；Host 已按 runbook 重启部署新代码（kill 旧 PID 20828 → start-syno.ps1 自监督 60s backoff 重拉，新 PID 24096，`/api/health` ok）；真实 vault 采样探针通过（2 近期 2026-08-13 + 2 久未回访 2026-05-19，sensitive 排除生效）。**验收阻塞项**：live preview 显示微信主动投递连续失败 64 次（`PROACTIVE_DELIVERY_EXHAUSTED`，target token 已约 5.5 天未刷新）——需主人发任意微信消息刷新持久 token 后卡片才会真实到达（runbook 见记忆：drain 用持久 token 自动重投）。已知行为：渠道中断期间灵感与其它 bundle 一样按日预算堆积重试（balanced=2 张/日上界），恢复后补投；连续失败熔断超出 D12 范围，记入后续观察。Owner 连续数日验收回填后 #11 封板。
- 2026-09-01：拆库①设计冻结完成（#14，`98cd3ed`）：全量探针 `PATHS.repoRoot`/`resolveInside`/`relativeToRepo` 全部消费点并分类为「知识根翻转」与「代码根保持」两类；junction 实证（`D:\tmp\junction-probe2`）否决 D11 的透明性设计点——checkout 历史提交穿透 junction 写入知识库目录（数据丢失通道），而 `git clean -fdx` 与 Node `fs.rm recursive` 对 junction 安全（只动链接）。冻结 D13.1~D13.13：物理迁出至 `SYNO_KNOWLEDGE_ROOT`（缺省回退单库模式，测试/fresh clone 零变化）、知识仓布局原样保持逻辑路径不变、GitGuard 默认根翻转+`productBranch:main` 断言（堵实证误伤类）、validator 双根（契约留代码仓）、workspace 默认值全面翻转、verify 双模式、cutover 窗口与回滚预案。关键实证摘录：GitGuard 构造已参数化（`{ repoRoot }`）；`readHeadText` 以 repoRoot 为 cwd 自包含；workflow-context-compiler 的 BASE_SOURCES 全为 `vault/99-System/**`；DSH skills 无绝对 vault 路径（桥接经 host，DSH 侧零改动）；`relativeToRepo` 对仓外路径抛 `PATH_OUTSIDE_ROOT`（job-store/knowledge-store/reports 必须换解析器，否则拆库即崩）。无代码改动，纯设计提交。
- 2026-09-01/02：拆库②迁移实施与 cutover 完成（#15）。代码改造 `24e97af`（15 files +195/−54）：`PATHS.knowledgeRoot`（env `SYNO_KNOWLEDGE_ROOT`，缺省回退 REPO_ROOT）+ `relativeToKnowledge`；GitGuard 默认根翻转为知识仓、`productBranch` 断言（symbolic-ref，未出生分支安全——rev-parse 实证会炸）、锁恒落代码仓 `.runtime/locks/`；validator 双根（生产接缝 agent-host 显式传 `contractRoot: PATHS.repoRoot`）；agent-host/ingest/runtime/server/knowledge-profile/workflow-context-compiler 的 workspace 与根默认值全面翻转；verify 双模式（required 清单永久移除 `vault/AGENTS.md`/`ops/README.md`，env 设置时追加知识仓形态检查）；`vault-migrate.mjs` 默认根迁移。实证修正 `97e557a`（7 files +31/−15）：**实例根优先于进程级 KNOWLEDGE_ROOT**——env 全局存在后，KnowledgeStore/JobStore/ReportService 对显式夹具根会被 `relativeToKnowledge` 边界误杀（PATH_OUTSIDE_ROOT），逻辑路径改由本实例 vault/ops 根的父目录求相对（KnowledgeProfileService 随之改为经 `knowledge.vaultRoot` 基准读正文，删除 repoRoot 参数）；知识检索集成测试（真实库）env 缺席时安静跳过；calendar 夹具子宿主显式 `SYNO_KNOWLEDGE_ROOT=fixture` 回落单库。cutover 窗口实测：停 supervisor+host → 全量 bundle 备份 `C:\tmp\syno-pre-split-24e97af.bundle`（36MB）→ robocopy 复制并校验 vault 666/ops 684 文件一致 → 知识仓 `D:\workSpace\syno-knowledge` `git init -b main` 初始提交 `7e0d0b0` → 代码仓删除提交 `60a8c43`（+`.gitignore` 防御性 `vault/`/`ops/` 条目）→ 用户级 `SYNO_KNOWLEDGE_ROOT` + planner vaultRoot 迁移 → `Start-ScheduledTask Syno` 重启（新 PID 7676，`/api/health` ok）。冒烟：`POST /api/syno/knowledge/profile` 真实 Job `job-20260901-77b57d20` completed，知识仓 main 落 3 个产品提交（merge/side-effects/complete），**代码仓 git status 为空=拆库目标结构性证明**；profile latest `fresh:true`（527 notes/463 searchable 实证活 vault 读取）；verify 双模式通过（bogus 路径反证 6 项报错）；全量回归双模式 713/713 与 712 pass+1 skip（集成测试跳过）。事故记录：一次 cwd 漂移把删除提交误打进知识仓，`reset --hard` 无损恢复，纪律已固化进记忆（跨仓库一律 `git -C` 绝对路径）。Host 运行代码=`60a8c43`（`97e557a` 为实例根修正，生产默认根下行为等价，随下次部署生效）。
- 2026-09-02：拆库③回归验证与文档封板（#16）。
- 2026-09-02：**#11 恢复事件（运行态事故与修复记录）**——Owner 反馈「发送微信并没回话」。排查：`weixin-poller.lock` 持有 9-1 01:16 的死进程 PID 11456，Windows 回收后该 PID 已属 `taskhostw.exe`；锁 plain `acquire` 只做 `processIsAlive`（不校验进程身份，`verifyIdentity` 仅 failFast/清理路径使用），判定「运行中实例持有」→ Host（含 cutover 后 23:26 重启）微信轮询器 30s 超时后从未启动 = **入站消息永远到不了管家，「发消息刷新 token」机制被它自己锁死**（出站 token 过期是另一条线；早晨 bundle 8 次 `PROACTIVE_DELIVERY_EXHAUSTED` 为并发症状）。修复：验证 11456 非管家进程后清除过期锁 → runbook 重启 Host（kill supervisor+node→`Start-ScheduledTask Syno`）→ 轮询器成功获锁：Owner 消息即时拉取（第一条触发会话重置刷新持久 token；第二条真实模型对话 `harness-run-2e4df919` 完成），两条均 `markProcessed`（=回复已发出），`weixin-runtime.json` mtime 更新（轮询持续写）。**代码级盲点（待后续任务，不在运行中改码）**：`process-lock.mjs` 默认 acquire 应以 `verifyIdentity:true` 核对 `processStartedAt`——PID 复用在长驻锁上是确定性死锁源；修复需加测试（进程启动时间身份），并入运行性修复批次。
- 2026-09-02：代码审查后续——Owner /code-review 深审拆库批次，四项整改落地 `1209028`（10 files +101/−69）：①**process-lock 身份基准确认**——`inspectProcessLock` 身份基准改为 `processStartedAt || createdAt`（旧世代锁 `{pid,createdAt}` 以锁创建时间兜底：复用 PID 启动必然晚于锁创建，>10s 即 stale）；plain acquire 同步升级（不再只信 `processIsAlive`）；微信轮询锁 `LocalProcessLock` 委托共享 `ProcessFileLock(failFast)` 并保留布尔契约（新增 recycled 双路径测试——pid 4 因 Get-Process 不可达，用自 pid + 身份时间前置 1h 精确建模）；②**relativeToRoot 实例根 confine**——`paths.mjs` 新原语对给定根求相对并保持越界断言（D13.6 修复曾丢失 PATH_OUTSIDE_ROOT），job-store/knowledge-store/reports 三处统一；③**verify 文件枚举 git 语义化**——`git ls-files` + `--others --exclude-standard` 取代白名单 walk（忽略规则一次维护在 .gitignore，测试产物不再漂移计数，quarantine 显式保留排除）；④**fallback 无 vault 启动护栏**——env 未设且代码仓无 vault/ 时 warning `runtime.fallback_no_vault`（非测试环境；fresh clone 合法形态不阻断）。回归：部署形态 715/715（+2 例）、回退形态 714+1 skip、`pnpm run verify` 341 files/8 docs（双形态）；fresh clone 终态 HEAD 通过。审查产出 0 Required、3 Optional+1 FYI——修复后全部关闭。三轴复审：Standards——拆库 diff（`98cd3ed..97e557a`）全量逐文件阅读：参数化缺省（`repoRoot`/`contractRoot`）与既有构造衔接一致、无残留双轨、无死代码（`relativeToKnowledge` 由 split 测试行使）；Spec——D13.1~D13.13 逐项核对代码/文档/运行态；Security——剩余 `PATHS.repoRoot` 消费点全部为代码根保持者（仓指纹、DSH supervisor/config/launcher、show-toast、check-active-docs），反证无知识内容泄漏代码仓；写入边界未被放宽（`contractRoot` 留代码仓=契约不随知识提交漂移）。**复审发现并补强一个闸门缺口**：`productBranch` 断言原只挡 `commitPaths`，worktree 合回（`mergeWorktree`）可在功能分支执行（合入旁路）——已把同一闸门前置到 `#mergeWorktree`（`391d654`），repo-split 测试补「功能分支合回被拒 + 回 main 放行」双断言（3/3 pass）。文档封板：AGENTS.md（项目事实源/执行器/知识任务按双仓改写，删除 D9 残留 `allowSelfModify` 表述）；README.md（知识仓指向）；docs/INDEX.md（状态段 + vault/AGENTS 与 ops/README 行标注知识仓）；docs/KNOWN-LIMITATIONS.md（拆库条目：旧分支地雷 `<60a8c43`、回滚保险、备份范围）；docs/ARCHITECTURE.md（Runtime locations 双仓化）；docs/OPERATIONS.md（状态分层双仓化 +「知识仓库拆分」runbook：环境变量/GitGuard/planner/地雷/回滚预案/旧历史 bundle；备份 step 3 改知识仓）；知识仓 README.md（定位与边界）。验收证据落知识仓 `ops/acceptance/repo-split/`。过程修正：AGENTS.md 首稿含本机绝对路径被 verify 扫描器拦截——机制留契约、具体路径归 docs（verify 卫生规则不放松）。当轮回归实测：拆库形态（env 设置）`pnpm test` 713/713 pass、0 fail、0 skip（知识检索集成用例真实库运行）；回退形态（env 缺席）712 pass + 1 skip（集成用例按设计安静跳过）；`pnpm run verify`（env 设置）通过——Repository verification 341 files（拆库前 1655，代码仓已零知识内容；计数排除测试产物日志——verify 扫描忽略集 .gitignore 语义化）、active docs 8 files；`git diff --check` 通过。fresh clone（`D:\tmp\syno-fresh-16`，HEAD `588dc4f`）：`pnpm install --frozen-lockfile` 成功（warm store 2s），env 缺席 `pnpm test` 712 pass + 1 skip、0 fail——零配置可跑 = D13.2 回退形态承诺兑现。**过程修正（verify 卫生纪律不放宽）**：AGENTS.md 与 NEXT_SESSION.md 首稿各含一处本机绝对路径，被 verify 扫描器拦截——机制留契约（AGENTS.md 已改指 runbook）、本机路径只进 docs/ 与运行记录，两文件修正后 verify 双形态通过。Phase E（#14~#16）全部关闭，剩余任务仅 #11 待 Owner 微信验收。
- 2026-09-03：#11 验收 Day 2 抓到结构性缺陷并当日修复（B1，`c0111c3`，5 files +150/−22）——12:30 卡未出，12:35 自检实证根因：cadence=balanced 每日预算 2 条被 07:30 孤儿维护提醒 + 08:01 早报耗尽，SignalEngine 预算门整体静默返回空（proactive.json lastEligibleSignals=0）；当日积压 21 条 event 信号每日清晨抢预算，预约日卡必死，非偶发。修复：①预约信号（morning/evening/inspiration/weekly）脱离事件型预算恒 eligible；②event 抑制每日记一笔 `proactive.signal.budget_suppressed`（消灭静默）；③预约投递不计数 notificationsToday；④同车 L0a tick 计时观测（completed{durationMs,outcome}/failed 附 durationMs，快速空转不记防刷 journal）。旧预算语义断言四处同步更新（provider-agent 三例 + proactive-reliability 事件序列含 tick.completed）。双形态回归 719/719、verify 341/8。部署实证：13:29 重启首拍（初始化后 10s）出卡、35s 生成、首投即成；同拍 budget_suppressed 记 21 条抑制。#11 验收口径：Day 2 记「缺陷发现+修复+补卡」，连续多日证据自 09-03 修复后重新累计。另立任务 #17（封板后串行：L0b tick 三段重构+组合租约 → L2 投递成功后回写 → P1 反馈工具化 inspiration.record_feedback；设计冻结于 plans/idempotent-knitting-toast.md）与 #18（本项）。
