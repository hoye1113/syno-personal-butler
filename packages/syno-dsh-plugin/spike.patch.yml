# Spike A overlay: mount capabilities plus the dev spike beside this patch.
# Not part of the production bundle; run with `--patch <this file>`.
- insert:
    - id: syno-capabilities
      name: './plugins/capabilities/index.mjs'
    - id: syno-capabilities-spike
      name: './plugins/capabilities/spike.mjs'
