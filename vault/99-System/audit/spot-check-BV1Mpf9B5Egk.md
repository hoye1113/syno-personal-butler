# Spot check 工作表：Claude Code实战：构建一个AI数据分析师

- **Vault**: `02-Resources/AI and Agents/B站视频知识库/Agent架构与平台/Claude Code实战-构建一个AI数据分析师.md`
- **时长**: 51:46
- **spot_check**: （未登记）
- **transcript_source**: `Recastory/workspace/bilibili-retranscribe/BV1Mpf9B5Egk/article.md`
- **ASR**: `${RECASTORY_WORKSPACE}\bilibili-retranscribe\BV1Mpf9B5Egk\article.md`
- **ASR 段数**: 57

## 相关阅读 wikilink
- ✓ 无死链（MOC 除外）

## 分话题 → ASR 锚点（自动；未命中 ≠ P0，需人工读段）

### 1. 数据分析师四步循环：AI 能接哪几棒

- `扔进 Claude Code 每天/每周跑` → _ASR 未命中（核对是否为讲义归纳）_
- `Agent` → Because the LLNs can actually interpret those things themselves, it can actually help you scale that to monitor.More than you might typicall
- `Slack` → Because the LLNs can actually interpret those things themselves, it can actually help you scale that to monitor.More than you might typicall
- `Linear` → Because the LLNs can actually interpret those things themselves, it can actually help you scale that to monitor.More than you might typicall
- `Google Doc` → to you.What a good story is, and so what's great is that you can kind of do those side investigations, those deep dives.As you start to craf
- `Claude Code` → title: "Claude Code Analyst"

### 2. 周报 vs Dashboard：为什么「有人读」 matters

- `「Claude 总会读、总会提 follow-up 问题」` → _ASR 未命中（核对是否为讲义归纳）_
- `data incident` → Access to more than just the data warehouse it has like the Slack access in a bunch other context.Hopefully we'll help it come with better i
- `interpretation + 跨源关联` → _ASR 未命中（核对是否为讲义归纳）_
- `Claude` → title: "Claude Code Analyst"
- `Brex` → Yeah, I feel like most companies don't have that setup at all, like so.So yeah, it's great that UN and products are doing this.Well, I think
- `Agent` → Because the LLNs can actually interpret those things themselves, it can actually help you scale that to monitor.More than you might typicall
- `Slack` → Because the LLNs can actually interpret those things themselves, it can actually help you scale that to monitor.More than you might typicall

### 3. 为什么用 MCP：Rails 给所有人同一起点

- `没有数据团队文档化的 join 模式` → _ASR 未命中（核对是否为讲义归纳）_
- `从同一套 rails 起跑` → _ASR 未命中（核对是否为讲义归纳）_
- `一个 domain 3 条 query` → _ASR 未命中（核对是否为讲义归纳）_
- `Snowflake query history / 现有 dashboard` → _ASR 未命中（核对是否为讲义归纳）_
- `Demo prompt 要素` → _ASR 未命中（核对是否为讲义归纳）_
- `3 条 eval 问题` → _ASR 未命中（核对是否为讲义归纳）_
- `capabilities（能问什么）` → _ASR 未命中（核对是否为讲义归纳）_
- `Snowflake` → some analysis.Example, so like two basic ones to really illustrate.How to.Build some of the metrics within the data and then I'd say want to

### 4. 数据分析 Agent 特有问题：Context 管理

- `Token 爆炸` → _ASR 未命中（核对是否为讲义归纳）_
- `LIMIT` → It's typically not going to run is a problem.In step one, and it actually could run into the problem in step one when you actually you task 
- `Tight semantic context` → to get wrong. .You can't just import your whole table into cloudude.When you say tight semantic context, you mean just like telling it which
- `表文档给 Agent 看` → _ASR 未命中（核对是否为讲义归纳）_
- `Glean MCP` → _ASR 未命中（核对是否为讲义归纳）_
- `每个连接建 subagent` → _ASR 未命中（核对是否为讲义归纳）_
- `不能直接 copy` → _ASR 未命中（核对是否为讲义归纳）_
- `Token` → think you over that yeah.I set up the kind of snowflake CLI, so it actually has access via that.And then to ensure it's a little bit more re

### 5. Live Demo：Series B 预测与 Cursor momentum

- `金额、行业（AI vs healthcare 61% vs 73%）、velocity query` → _ASR 未命中（核对是否为讲义归纳）_
- `Cursor #1` → _ASR 未命中（核对是否为讲义归纳）_
- `丢 thread` → _ASR 未命中（核对是否为讲义归纳）_
- `Brex 支出侧` → _ASR 未命中（核对是否为讲义归纳）_
- `startup + enterprise 双杀` → _ASR 未命中（核对是否为讲义归纳）_
- `startup 产品内嵌 agent 选 Claude` → _ASR 未命中（核对是否为讲义归纳）_
- `voice 附加首选` → _ASR 未命中（核对是否为讲义归纳）_
- `三条种子 query` → _ASR 未命中（核对是否为讲义归纳）_
- ⚠ **本节无自动命中**，优先人工对读

### 6. 护栏：Skills 防 PM 拖垮库

- `random query 打挂库` → _ASR 未命中（核对是否为讲义归纳）_
- `token 成本失控` → _ASR 未命中（核对是否为讲义归纳）_
- `data analysis Skill` → do you have any controls?In place.Yeah, I think that's.That's something we're at.I find like.The new thing that clterly skills actually kind
- `computational story mapping` → do you have any controls?In place.Yeah, I think that's.That's something we're at.I find like.The new thing that clterly skills actually kind
- `join  leaps` → _ASR 未命中（核对是否为讲义归纳）_
- `MCP + Skills 护栏 + 数据人可见的 join 披露` → _ASR 未命中（核对是否为讲义归纳）_
- `Skill` → do you have any controls?In place.Yeah, I think that's.That's something we're at.I find like.The new thing that clterly skills actually kind
- `Self` → Because the LLNs can actually interpret those things themselves, it can actually help you scale that to monitor.More than you might typicall

### 7. 入门三步（TL;DR）

- `2–3 条真实在用的 core queries` → _ASR 未命中（核对是否为讲义归纳）_
- `context` → Because the LLNs can actually interpret those things themselves, it can actually help you scale that to monitor.More than you might typicall
- `同一套 context 灌进 Hex 等 BI` → _ASR 未命中（核对是否为讲义归纳）_
- `semantic layer for agents` → _ASR 未命中（核对是否为讲义归纳）_
- `Claude Code MCP` → _ASR 未命中（核对是否为讲义归纳）_
- `AI BI` → _ASR 未命中（核对是否为讲义归纳）_

## 原话抽查（英文片段 ≥4 词）

- ✓ `Recreating the way a data scientist`
- ⚠ `Claude will always read it Claude`
- ⚠ `One query might return million rows`
- ⚠ `If you start to give it`
- ⚠ `Those three or four queries really`
- ✓ `Customers on our platform have already`

## 人工结论（P0 清零后归档 audit/）

- P0：
- P1：
- 通过：是 / 否
- 通过后 frontmatter 加：`spot_check: YYYY-MM-DD`